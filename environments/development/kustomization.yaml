apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: application-development

resources:
  - ../../application
  - namespace.yaml
  - ingress.yaml

replicas:
  - name: nginx-app
    count: 1

configMapGenerator:
  - name: web-content
    files:
      - index.html
    options:
      disableNameSuffixHash: true

secretGenerator:
  - name: app-secret
    literals:
      - APP_ENV=development
      - APP_USER=dev_user
      - APP_TOKEN=dev-token-123
    type: Opaque
    options:
      disableNameSuffixHash: true

patchesStrategicMerge:
  - patch-resources.yaml
